.row
  .col-xs-12
    %table.table.table-bordered

      %tr
        %th Your Sites
      - @sites.each do |site|
        %tr
          %td
            = "http://#{site.name}.kissr.com/"
            = link_to site_path(site), method: 'delete', class: 'btn btn-danger btn-circle' do
              .span.glyphicon.glyphicon-remove
            = link_to "http://#{site.name}.kissr.com/", method: 'delete', class: 'btn btn-default btn-circle', target: :_blank do
              .span.glyphicon.glyphicon.glyphicon-share
%p.clearfix
  = link_to 'Create a Site', new_site_path, class: 'btn btn-primary pull-right'
